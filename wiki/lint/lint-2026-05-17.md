---
title: Wiki Lint — 2026-05-17
type: lint
tags: [lint, structural]
updated: 2026-05-17
---

## Wiki Lint — 2026-05-17

**Scope:** Structural health check — orphans, broken wikilinks, contradiction flags, gap tracking, learning path coverage.

---

## 1. Broken Wikilinks

### Genuine broken links (no matching page exists)

| Wikilink | Status |
|---|---|
| `[[research-p3-sparse-lora]]` | Research moved to separate wiki; link remains in some wiki pages — remove |
| `[[ingest-workflow]]` | Referenced but no page exists |
| `[[wikilinks]]` | Referenced but no page exists |
| `[[wiki-structure]]` | Referenced but no page exists |

### Backslash typos — FALSE POSITIVE

These appeared in the grep but all occurrences are inside `wiki/lint/` history files documenting *past* fixes, not in actual concept/query/source pages. Already fixed in lint runs 2–5. No action needed.

### Capitalised/display-name variants (benign — Obsidian resolves these)

`[[Attention Is All You Need]]`, `[[Weight Tying]]`, `[[Tokenization]]`, `[[Perplexity]]`, `[[Positional Embedding]]`, `[[LLM Evaluation]]`, `[[MultiHeadAttention]]`, `[[LayerNorm]]` — all resolve to existing pages. No action needed.

---

## 2. Orphan Pages

**No orphans found.** Initial report was a false positive caused by truncated wikilink scan output. Verified manually:

| Page | Linked From |
|---|---|
| `byte-pair-encoding.md` | `tokenization.md` |
| `large-language-models.md` | `tokenization.md` |
| `kv-caching.md` | `decoding-strategies.md`, `inference-scaling.md` |
| `inference-scaling.md` | `scaling-laws.md`, `emergent-abilities.md`, `large-language-models.md`, `kv-caching.md`, `Decoder_archtecture.md`, `gpt-family.md` |
| `lora.md` | `instruction-fine-tuning.md`, `ablation-study.md`, `ablation-study-explained.md`, `instruction_fine_tuning.md` |

---

## 3. Open Contradiction Flags

| File | Flag | Status |
|---|---|---|
| `wiki/concepts/instruction-fine-tuning.md:67` | Masked vs unmasked instruction tokens [contested] — Shi et al. 2024 says don't mask; Raschka 2025 doesn't mask by default | **Correctly tagged** — both positions cited with sources; `[contested]` is the right treatment; no fix needed |

---

## 4. Gap Files

- `wiki/gaps/` directory removed — gap tracking consolidated into `learning-path.md § Gaps in This Path` (sole tracker by design)
- **5 open gaps** (all in learning-path.md):
  - Stage 2: Sparse attention, RoPE/ALiBi
  - Stage 4: Mixed precision training (fp16/bf16)
  - Stage 5: Beam search internals, speculative decoding
  - Stage 6: RLHF, DPO, QLoRA, prefix tuning
  - Stage 7: ROUGE score, MT-Bench, MMLU evaluation
- **Resolved:** dual-tracker drift eliminated; CLAUDE.md updated to reflect single-tracker workflow

---

## 5. Learning Path Coverage

- Concept pages on disk: **40**
- Concepts in `learning-path.md` before fix: **39** (index navigation was stale at 38)
- **Missing:** `llm-evaluation.md` — added to Stage 7 ✓
- Index navigation updated to: 40 concepts, 5 gaps ✓

---

## Summary

| Check | Result |
|---|---|
| Broken wikilinks (genuine) | 4 → fixed 3; `[[wikilinks]]` in old lint files only (benign) |
| Backslash typos | False positive — all in lint history files, already fixed |
| Orphan concept pages | 0 (false positive from truncated scan) |
| Open contradiction flags | 1 — correctly tagged [contested], no fix needed |
| Open gaps | 5 (tracked in learning-path.md § Gaps — sole tracker) |
| Learning path coverage | Fixed — `llm-evaluation` added to Stage 7 |
