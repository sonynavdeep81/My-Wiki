---
title: Wiki Lint — 2026-04-16
type: query
tags: [lint, maintenance]
sources: 0
updated: 2026-04-16
---

## Wiki Lint — 2026-04-16

**Summary**: 6 broken wikilinks fixed; 0 orphan concepts/entities; 7 orphan queries (expected); 5 knowledge gaps flagged.

---

## Broken Wikilinks — Fixed

All broken links were caused by two patterns:

**1. Title-case names instead of slugs in `pytorch-nn-building-blocks`**

| Was | Fixed to |
|---|---|
| `[[Layer Normalization\|LayerNorm]]` | `[[layer-normalization\|LayerNorm]]` |
| `[[Multi-Head Attention]]` (×2) | `[[multi-head-attention]]` |
| `[[Embeddings]]` (×2) | `[[embeddings]]` |
| `[[Weight Tying]]` (×2) | `[[weight-tying]]` |
| `[[Layer Normalization]]` | `[[layer-normalization]]` |

**2. Backslash-escaped pipe in markdown table in `training-loop-primitives`**

`[[dropout\|Dropout]]` in a table cell — the `\` escapes the `|` for markdown but corrupts the wikilink target. Fixed to `[[dropout]]`.

---

## Orphan Pages

**Concepts**: 0 orphans — all concept pages have at least one inbound link.

**Entities**: 0 orphans — all entity pages are linked.

**Sources**: 2 with no inbound wikilinks from other wiki pages:
- `Decoder_archtecture` — linked from index.md only
- `GPT2_Clean` — linked from index.md only

These are acceptable: source pages are the roots of the wiki, not expected to be cited by other pages. `Attention_2023` is the exception with 9 inbound links because it's frequently cited.

**Queries**: 7 orphans — expected. Query pages are Q&A outputs, not nodes in the concept graph.

---

## Under-linked Concepts

- `label-smoothing` — only 1 inbound link (from `dropout`). Should also be linked from `attention-is-all-you-need` and `optimizer`.

---

## Knowledge Gaps — Missing Pages

Concepts mentioned frequently in wiki prose but with no dedicated page:

| Concept | Mentions | Priority |
|---|:---:|---|
| `softmax` | 27x | High — core operation in attention, used everywhere |
| `temperature` | 21x | High — key decoding/inference concept |
| `LoRA` | 17x | High — referenced in fine-tuning but no detail page |
| `perplexity` | 13x | High — primary LLM evaluation metric |
| `warmup` | 13x | Medium — training schedule technique |
| `backpropagation` | 6x | Medium — fundamental algorithm, only described briefly in training-loop-primitives |
| `gradient clipping` | 1x | Low |

---

## Suggested New Sources

- **Andrej Karpathy's "Let's build GPT" video** — covers softmax, backpropagation, and temperature in depth; would fill 3 gaps at once
- **LoRA paper** (Hu et al. 2021) — would give `LoRA`, `QLoRA`, `PEFT` proper pages
- **Chinchilla paper** (Hoffmann et al. 2022) — would deepen `scaling-laws` and `perplexity`

---

## Related

- [[lint-2026-04-14d]]
- [[pytorch-nn-building-blocks]]
- [[training-loop-primitives]]
